{
  "name": "root",
  "private": true,
  "scripts": {
    "link": "lerna link --force-local",
    "build": "npm run link && lerna run build --stream",
    "test": "npm run build && lerna run test --stream"
  },
  "devDependencies": {
    "@tsconfig/node10": "^1.0.7",
    "@types/glob": "^7.1.3",
    "@types/uuid": "^8.3.0",
    "lerna": "^3.22.1",
    "ts-node": "^9.1.1",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "@katalon/testops-commons": "0.0.1-beta.12"
  }
}
